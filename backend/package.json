{
  "name": "mirror-backend",
  "version": "0.0.0",
  "description": "Mirror API Lambda function",
  "main": "dist/handler.js",
  "scripts": {
    "build": "tsc",
    "package": "npm run build && cd dist && zip -r ../function.zip .",
    "dev": "tsc --watch",
    "start:local": "npm run build && node dist/local-server.js",
    "dev:local": "tsc && node dist/local-server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@aws-sdk/client-lambda": "^3.0.0",
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.0",
    "@types/node": "^20.0.0",
    "@types/express": "^4.17.17",
    "@types/cors": "^2.8.13",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
} 